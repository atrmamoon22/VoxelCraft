#version 330 core

layout (location = 0) in vec2 aPos;

out vec2 textureCoords;

uniform mat4 transformationMatrix;

void main(void) {

    gl_Position = transformationMatrix * vec4(aPos, 0.0, 1.0);
    textureCoords = (aPos+1.0)/2.0;
}